# =============================================================================
# 阿里云百炼 API 配置
# =============================================================================
# 获取方式：访问 https://bailian.console.aliyun.com/ 创建应用获取 API Key
# 格式: sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
DASHSCOPE_API_KEY=sk-your-dashscope-api-key

# =============================================================================
# AgentRun Sandbox 配置
# =============================================================================
# 获取方式：访问 https://agentrun.ai/ 注册账号并创建 Sandbox 模板
# 格式: sandbox-aio-xxxxxx
AIO_TEMPLATE_NAME=your-aio-template

# 可选配置：指定 Sandbox 名称
SANDBOX_NAME=my-sandbox

# =============================================================================
# VNC Server 配置（可选）
# =============================================================================
# VNC Server URL（默认: http://localhost:8080）
VNC_SERVER_URL=http://localhost:8080

# =============================================================================
# 本地模式配置（可选）
# =============================================================================
# 开启本地模式（绕过 30 秒超时限制）
# 本地模式需要在本地运行 Sandbox 服务（端口 5000）
# true: 使用本地模式，false: 使用远程云服务（默认）
LOCAL_MODE=false

# =============================================================================
# 调试配置（可选）
# =============================================================================
# 开启 DEBUG 日志
# AGENTRUN_SDK_DEBUG=1

# =============================================================================
# 高级配置（通常不需要修改）
# =============================================================================
# AgentRun 控制端点（通常不需要修改）
# AGENTRUN_CONTROL_ENDPOINT=agentrun.cn-hangzhou.aliyuncs.com

# AgentRun 数据端点（通常不需要修改）
# AGENTRUN_DATA_ENDPOINT=https://<account-id>.funagent-data.cn-hangzhou.aliyuncs.com

# Alibaba Cloud 访问密钥（仅在需要直接调用阿里云 API 时使用）
# ALIBABA_CLOUD_ACCESS_KEY_ID=your-access-key-id
# ALIBABA_CLOUD_ACCESS_KEY_SECRET=your-access-key-secret
# ALIBABA_CLOUD_REGION=cn-hangzhou
